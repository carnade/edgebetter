# Secrets — never commit
.env
.env.local
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

# Node
node_modules/
dist/
.vite/
*.tsbuildinfo

# Data / runtime
pgdata/
# Re-downloadable nflverse release assets. Cache-first by design, so a clone simply
# refetches what it needs rather than carrying megabytes of derivable data in git.
.nflverse_cache/
*.sqlite
*.db
.backfill_state.json

# Editors / OS
.DS_Store
.idea/
.vscode/

# Database dumps from scripts/backup.sh
backups/
